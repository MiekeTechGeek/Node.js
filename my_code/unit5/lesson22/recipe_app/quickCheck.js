// Quick check 22.1 How does a cookie’s secret key change the way that data is sent and stored on a browser?
// = The secret key used with cookies allows data to have some encryption. Encryption
// is important for securing the data sent over the internet and for making sure that the data living in the
// user’s browser isn’t exposed to modifications. 

// Quick check 22.2 What two arguments are needed for the req.flash method?
// = req.flash needs a flash-message type and a message.